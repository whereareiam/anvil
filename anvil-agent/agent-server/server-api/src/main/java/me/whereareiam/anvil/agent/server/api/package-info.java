/**
 * Embedded agent contracts for platform services, authenticated endpoints, and native handlers.
 * Platform plugins and external operation providers implement these contracts inside a Minecraft
 * server or proxy, sharing operation descriptors and payloads with host-side clients.
 */
package me.whereareiam.anvil.agent.server.api;
