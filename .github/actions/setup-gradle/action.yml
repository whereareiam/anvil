name: Set up Gradle
description: Install Java toolchains and configure Gradle caching.

inputs:
  java-version:
    description: Java version to install.
    required: false
    default: "25"
  java-distribution:
    description: Java distribution to install.
    required: false
    default: temurin
  cache_read_only:
    description: Set true to prevent Gradle from writing shared caches; auto preserves the event defaults.
    required: false
    default: auto

runs:
  using: composite
  steps:
    - name: Set up JDK ${{ inputs.java-version }}
      uses: actions/setup-java@v5
      with:
        java-version: ${{ inputs.java-version }}
        distribution: ${{ inputs.java-distribution }}
        check-latest: true

    - name: Set up Java 21 compiler toolchain
      if: inputs.java-version != '21'
      uses: actions/setup-java@v5
      with:
        java-version: "21"
        distribution: ${{ inputs.java-distribution }}

    - name: Set up Gradle
      uses: gradle/actions/setup-gradle@v5
      with:
        cache-read-only: ${{ inputs.cache_read_only == 'true' || (inputs.cache_read_only == 'auto' && github.event_name != 'release' && github.event_name != 'workflow_dispatch' && github.ref != 'refs/heads/dev') }}
